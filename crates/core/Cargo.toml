[package]
name = "proxy-common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] } # Konfigürasyon veya IPC için serileştirme
toml = "0.8"
thiserror = "2"
